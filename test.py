from unittest import TestCase

__author__ = 'stgo'


class TestCalculations(TestCase):
   def setUp(self):
      self.logPoint()
      self.Pointonscanline = [x, y, z]
      self.Pointoffscanline = [x, y, z]
      self.Igmfile = igmfile
      self.centrepixel = [x, y, z]
      self.Pointonscanline = [x, y, z]
      self.Pointoffscanline = [x, y, z]